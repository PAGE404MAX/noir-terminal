# noir-terminal
a python script with just miscellaneous things in it. "face detector" "face redactor" "Stype" and "composer" "metadata viewer/editor"  this is a WIP and open source so feel free to use if wanted.
